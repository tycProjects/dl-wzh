# HOÀNG MOD — Shizuku file copier

Giao diện dark/neon theo mẫu ảnh người dùng cung cấp.

- Key miễn phí tự sinh, hiệu lực 24 giờ.
- Có bộ đếm ngược thời gian hết hạn.
- Khi hết hạn, màn hình trạng thái key được làm mới và hệ thống gửi thông báo.
- START yêu cầu key hiện tại + Shizuku.
- START chép `localconfig.json` và `Assembly-CSharp-patch.bytes` vào:
  `/sdcard/Android/data/com.dts.freefireth/files/`

Android 13+ cần cấp quyền thông báo để nhận thông báo hết hạn.

Sau khi chép file thành công, nút START sẽ tự mở ứng dụng có package `com.dts.freefireth`.

Sau khi START chép file thành công, app tự mở package `com.dts.freefireth` nếu Free Fire đã được cài đặt.

Nhà phát triển hiển thị: **HOANG MOD MẠNH NHẤT LỊCH SỬ**.

Key vĩnh viễn: `HOANG-PREMIUM` — key này không hết hạn và bỏ qua bộ đếm 24h.
